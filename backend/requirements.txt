fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.9.2
httpx==0.27.2

# DB
sqlalchemy==2.0.34
psycopg2-binary==2.9.9   # Postgres driver (used only when DATABASE_URL=postgres://...)

# LLM
openai==1.51.0
langchain==0.3.0
langchain-openai==0.2.0
langchain-core==0.3.5
langchain-community==0.3.0
langchain-groq==0.2.0           # optional — for free Llama 3.3 via Groq

# PDF resume parsing
pypdf==5.0.0

# Auth (optional Supabase JWT verification)
pyjwt[crypto]==2.9.0

# Error tracking (optional — env-gated)
sentry-sdk[fastapi]==2.16.0
